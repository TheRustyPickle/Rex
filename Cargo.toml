[workspace]
members = ["core", "db", "tui"]

resolver = "2"

[workspace.dependencies]
db = { path = "db" }
core = { path = "core" }
chrono = "0.4.41"

# The profile that 'cargo dist' will build with
[profile.dist]
inherits = "release"
